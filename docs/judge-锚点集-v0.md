# 判官人工锚点集 v0（judge-anchor-v0）

> 状态：v0 类目登记——首个类目 F1 于 2026-09-06 从 EQ-Bench 失败类提炼入册。
> 代码侧登记：`src/declared/contract.ts` `JUDGE_ANCHOR_SET_V0`（类目表）+ `JudgeFingerprint.anchorSetV`（判官指纹必填字段）。
> 用途：判官上岗前逐类目施测、偏差入账（R3-16 第 4 条「人工锚点集定期校准判官偏差」的实体）。
> 铁律在案：**EQ-Bench 分数本身不追**——它的失败模式只用来提炼判官校准类目，不是分数目标。

## 使用方式

1. 判官候选（异构 panel，≥2 家族）对锚点集逐类目作答；
2. 与人工参考比对，逐类目记偏差（仪器事件，`instrument: true`，走 bench 轨）；
3. 偏差超阈的类目 = 该判官的禁用面（相应盲评题不给它上桌）；
4. 锚点集本身冻结版本号；加类目 = 升版本（judge-anchor-v1），已入账偏差不追溯混算。

## 类目

### F1 · 表面在场虚高（zero-presence inflation）

- **定义**：文本中出现某情绪的词汇/场景线索（提到愤怒的词、哭的画面），但线索不属于被判主体——属于对方、属于过去、属于反讽或转述——主体实际不持有该情绪。**0 分语义 = 该情绪在主体的真实内在状态中缺席：线索在场 ≠ 主体在场。**
- **来源**：EQ-Bench v2 首跑（doubao-seed-evolving，60 题）三题负分失败类的共同模式——参考答案 0 分的情绪被判官型模型打出 8–9 分（题 66/112/152：`ref=(0,8,0,6)→user=(9,6.5,0.5,3.5)` 等）。
- **测法**：锚点题为「线索在场但主体缺席」的对话；判官须给该情绪 ≤2/10（人类参考 0–1）。打 5 分以上 = F1 偏差阳性。
- **为什么判官必须测**：C_s 盲评判的是「回应与信号的内容关联性」——F1 阳性的判官会把「堆了情绪词但没接住对方」的回应读成高关联，谄媚（R3-16）从词汇层后门回流。此为单判官不可上岗的第三个理由（前两个：EA 面弱、文风谄媚偏好）。

## 待建（判官上岗前）

- [x] F1 具体题集（2026-09-07 冻结 `anchor-f1-v0`：12 题 = zh 7 + en 5；10 题缺席 ref=0 + 2 题 trace ref=1——trace 题防「永远 0 分」的退化判官白卷；人工判例冻在题内。`ANCHOR_F1_SET_V0`，`src/declared/exams/anchorf1.ts`）
- [x] 施测 runner（考场第四卷 `run.ts f1`，只对判官候选开放；提示词 `f1-anchor-prompt-v1` **保持中性**——0 锚定义是通用标尺，不出现线索归属教练：塞考点测出的是合规不是偏差）
- [x] 偏差入账接线（runner 产出对齐 `recordJudgeAnchorDeviation` → bench_runs，kind=`judge_anchor_deviation`，instrument 事件；测试锁定往返）
- [ ] 其余类目（候选：冗长偏好、顺从偏好、锚点漂移——R3-16 判词点名的前两项；按首跑数据提炼）
- [ ] 异构多判官 panel 上岗（单判官裁决自动 provisional；F1 阳性判官禁入 C_s 类盲评）

## 首测基线（2026-09-07，doubao-seed-evolving，单判官 provisional）

anchor-f1-v0 × f1-anchor-prompt-v1，temperature 0，12/12 可解析：

- **F1 阳性未触发（0/12 inflated）**，C_s 盲评资格保留；meanScore 1.17 / meanDeviation **+1.0**。
- **两处真实信号 → 偏差挂牌（跟判官指纹走，`JUDGE_BIAS_TAGS_V0`）：**
  1. `reported_cue_empathy_contagion`——转述类共情沾染：f1-zh-04（妈妈在电话里哭，ref 0）打 4 分。转述是五种归属形态里最难的，此面随判官指纹入账，panel 时按此对账补偿/禁用；
  2. `trace_signal_magnitude_inflation`——trace 弱信号（ref 1）被读高 2–3 档：虚高不是开关式是量级式，弱信号会被放大读。
- **反号成对入册**：判官侧把弱负价信号读高（本卷），declare 侧分类器把 fear 读低（−0.30）——同册不互抵，不同仪器不同方向，混账会掩盖各自机制。

### 解读边界（判词 2026-09-07，防过度宣读）

- **本卷是按锚点定义自造的「设计题」**，归属线索比 EQ-Bench 那种自然分布的野题更显性、题面更干净——虚高未复现，一部分可能来自题集干净而非判官干净。
- 结论只到「**C_s 盲评资格保留**」，不到「此判官无 F1 偏差」（尚未证明）。
- **真正的判别力在 panel 对照**：同一卷、两个家族，分歧面才是信息量——单判官的 0/12 只是候选测量。
- 排期裁决：第二家族判官人选归她定（网关里有什么家族就用什么，`--model` 跑同一卷）；判官考试不挡 A1 主线。

---

*锚点集是判官的考卷，不是模型的成绩单——它校准的是零件（判官选择与偏差补偿），永不回流引擎。*
